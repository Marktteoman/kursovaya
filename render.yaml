# render.yaml
services:
  - type: web
    name: genius
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate
    startCommand: gunicorn KursachxDDDDDDDDDDDDD.wsgi:application --bind 0.0.0.0:8000